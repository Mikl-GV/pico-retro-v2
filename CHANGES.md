# Журнал изменений pico-retro

Ветка: `cursor/windows-build-and-menu-fixes`

## 2026-08-21 — Atari 2600: переход на ядро Virtual VCS (MCUME) + F4

### Ядро Atari: Virtual VCS (x2600) из MCUME
- Заменён примитивный x2600 (dgrubb, `atari2600/`) на порт **Virtual VCS / x2600
  из MCUME** (`atari2600_mcume/`): полный 6507 с недокументированными командами,
  таймер RIOT, пэддлы, банки F8/F6/F4, SuperChip (F6SC), Parker Brothers (E0).
- Отрезана вся MCUME-обвязка (emuapi/pico_dsp/FatFs/дисплей): вместо неё —
  `system_atari_mcume.cpp` + `emu_stubs.h` (наш дисплей/кнопки/статическая память).
- Файлы ядра собираются как **-std=gnu89** (код 1996 г.: implicit int,
  `__inline`, GNU-расширения).

### Банкинг F4 (32 КБ) — добавлен
- В оригинальном MCUME нет F4 (только F8/F6/E0/FA/F6SC). Добавлен case 6:
  8 банков по 4 КБ, hotspot'ы `$0FF4..$0FFB`, при чтении и записи.
- Автоопределение банка по размеру ROM: 8К→F8, 16К→F6, 32К→F4.
- Это чинит **DK Arcade (32К F4)**, который не работал на старом ядре.

### Память
- ROM Atari читаются напрямую из flash (XIP) через `theCart` — экономия ~32 КБ
  RAM. Только 2К-карты (Air-Sea Battle) зеркалятся в буфер `cart_small[4096]`.
- RAM: ~241 КБ из 264 КБ (InfoNES + Virtual VCS вместе).

### Прочее
- `build.ps1` переведён на ASCII (PS 5.1 ломался на UTF-8 кириллице).
- Старая папка `atari2600/` не компилируется — используется только для
  `rom_*.h` (вшитые ROM Atari).

## 2026-08-21 — Переход на InfoNES + UI «9999-in-1»

### Ядро: InfoNES вместо самописного эмулятора
- Заменён самописный `nes.c`/`cpu6502.c` на ядро **InfoNES** из Murmulator
  (`infones/`): полный 6502, PPU, APU и **138 мапперов**.
- Это сняло проблемы совместимости: теперь работают SMB, Contra (VRC2),
  Little Nemo (MMC3) и др.
- Убраны `include/nes.h`, `include/cpu6502.h`, `src/nes.c`, `src/cpu6502.c`.

### Системный слой (`infones/system_pico_retro.cpp`)
- Рендер через streaming в GRAM дисплея (без framebuffer).
- Кнопки onboard → `InfoNES_PadState` (полярность NES).
- Выход из игры — удержание Start.

### Меню в стиле «9999-in-1»
- Фиолетовый фон со звёздами.
- Оранжевые градиентные боковые панели.
- Киноплёночные бордюры: 8 кадров 25×25 с каждой стороны, миниатюры игр,
  сплошная перфорация 2×2 с обеих сторон кадров.
- Логотип «PICO-RETRO» (8×8, scale 2), подсказка внизу.
- Прозрачный текст меню (не закрашивает фон со звёздами).
- Строки меню: 8px текст + 3px отступ (шаг 11px).

### Миниатюры игр
- `include/thumbs.h` — 15 миниатюр 25×25, сгенерированы нейросетью,
  конвертированы в NES-палитру (64 цвета). Хранятся в flash, RAM не занимают.

### Выводы
- UART отладка удалена — освобождены GP16/GP17.
- Подсветка дисплея (GP11) — жёстко на 3V3, не управляется.
- Аудио — моно PWM на **GP13** (RC-фильтр).
- Свободны: **GP11, GP14, GP28**.

### Дисплей
- `display.c` переписан: все функции пишут напрямую в GRAM (streaming),
  framebuffer 150 КБ удалён.
- Добавлен `display_text_at` / `display_text_at_nobg` (пиксельные координаты).

### Прочее
- Фон меню, бордюры, перфорация — см. `draw_film_strip` в
  `infones/system_pico_retro.cpp`.

## 2026-08-20 — Меню, About, разгон, мапперы (историческая запись)

- Скролл меню больше не уходит в отрицательный индекс.
- Разгон 250 МГц: vreg 1.20 В + пауза 10 мс + `set_sys_clock_khz(250000, true)`.
- Исправлена полярность кнопок `$4016` (самопроизвольное движение).
- PPU: `$2002` очищает все флаги, pre-render `v ← t` на строке 261.
- Мапперы: VRC2 (PRG 8К + CHR 1К), MMC3 (банки/IRQ), Camerica 71.
